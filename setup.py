from distutils.core import setup
setup(
    name = 'PyMysqlPool',
    packages = ['PyMysqlPool'], # this must be the same as the name above
    version = '0.2',
    description = 'python practical mysql pool -motivation from=>[lost connection to MySQL server during query]  base on mysql-connector ',
    author = 'Lucifer Jack',
    author_email = 'lujunxucuc@gmail.com',
    url = 'https://codeload.github.com/LuciferJack/python-mysql-pool/zip/master', # use the URL to the github repo
    download_url = 'https://github.com/LuciferJack/python-mysql-pool/archive/0.2.tar.gz', # I'll explain this in a second
    keywords = ['mysql pool', 'queue', 'support list'], # arbitrary keywords
    classifiers = [],
)